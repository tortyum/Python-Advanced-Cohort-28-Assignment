 
‎.github/workflows/automerge.yml
+12-3Lines changed: 12 additions & 3 deletions
Original file line number	Diff line number	Diff line change
@@ -4,6 +4,7 @@ on:
  pull_request:
    types: [opened, synchronize]

# Give GITHUB_TOKEN as much power as possible
permissions:
  pull-requests: write
  contents: write
@@ -12,8 +13,8 @@ jobs:
  auto-merge:
    runs-on: ubuntu-latest
    steps:
      - name: Select Token
        id: select-token
      - name: Pick Token (GITHUB_TOKEN if same repo, PAT if fork)
        id: pick-token
        run: |
          if [[ "${{ github.event.pull_request.head.repo.full_name }}" == "${{ github.repository }}" ]]; then
            echo "token=${{ secrets.GITHUB_TOKEN }}" >> $GITHUB_OUTPUT
@@ -24,6 +25,14 @@ jobs:
      - name: Enable auto-merge
        uses: peter-evans/enable-pull-request-automerge@v3
        with:
          token: ${{ steps.select-token.outputs.token }}
          token: ${{ steps.pick-token.outputs.token }}
          pull-request-number: ${{ github.event.pull_request.number }}
          merge-method: squash
      - name: Comment success
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ steps.pick-token.outputs.token }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ✅ Auto-merge has been enabled for this PR.
